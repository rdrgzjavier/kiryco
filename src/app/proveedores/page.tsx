import { providers } from "@/lib/mock-data";
export default function ProveedoresPage() { return <div className="page-container py-10"><h1 className="mb-4">Proveedores para familias</h1><p className="mb-8 text-warm-700">Planes freemium preparados para profesores, academias, clubes, tiendas y servicios locales.</p><div className="grid gap-5 md:grid-cols-3">{["Gratuito", "Destacado", "Premium"].map((p) => <div key={p} className="card p-6"><h2>{p}</h2><p className="text-warm-600">Perfil, visibilidad local y estructura lista para pagos.</p></div>)}</div><h2 className="mt-12 mb-4">Proveedores mock</h2>{providers.map((p) => <article key={p.id} className="card p-5"><b>{p.businessName}</b><p>{p.description}</p></article>)}</div>; }

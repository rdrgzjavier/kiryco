"use client";
import { useState } from "react";
import { categories, municipalities } from "@/lib/mock-data";
export default function PublicarPage() { const [ok, setOk] = useState(false); const [sent, setSent] = useState(false); return <div className="page-container max-w-3xl py-10"><h1 className="mb-4">Publicar</h1>{sent ? <div className="alert-info">Publicación enviada a revisión. Estado: pending_review.</div> : <form className="card grid gap-5 p-6" onSubmit={(e) => { e.preventDefault(); setSent(true); }}><label><span className="label">Categoría</span><select className="select" required>{categories.map((c) => <option key={c.id}>{c.name}</option>)}</select></label><label><span className="label">Municipio</span><select className="select" required>{municipalities.map((m) => <option key={m.id}>{m.name}</option>)}</select></label><label><span className="label">Título</span><input className="input" required /></label><label><span className="label">Descripción</span><textarea className="textarea" required /></label><label className="flex gap-3 text-sm"><input type="checkbox" checked={ok} onChange={(e) => setOk(e.target.checked)} required />Confirmo que esta publicación no incluye datos personales de menores, fotografías de menores ni información sensible.</label><button className="btn-primary" disabled={!ok}>Enviar a revisión</button></form>}</div>; }
